import { View, Text } from 'react-native'
import React from 'react'
import HelpSupport from '@/screens/help-support/help-support.screen'

export default function index() {
  return (
    <HelpSupport/>
  )
}